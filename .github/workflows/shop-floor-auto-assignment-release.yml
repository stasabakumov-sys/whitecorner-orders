name: Shop Floor automatic composition release
on: workflow_dispatch
permissions:
  contents: read
concurrency:
  group: shop-floor-auto-assignment-release
  cancel-in-progress: false
jobs:
  verify-and-migrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Verify migration
        run: |
          npm install --prefix "$RUNNER_TEMP/shop-auto-db" --no-save --ignore-scripts @electric-sql/pglite@0.5.8
          node .github/scripts/shop-floor-auto-assignment.test.mjs "$RUNNER_TEMP/shop-auto-db/node_modules/@electric-sql/pglite/dist/index.js"
      - name: Apply automatic composition schema
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
        run: node .github/scripts/shop-floor-auto-assignment-release.mjs --apply
