import { Component, signal } from '@angular/core'; import { AuthService } from '../../core/services/auth.service';
@Component({selector:'app-login',standalone:true,template:`<div class="wrap"><form (submit)="submit($event)"><h2>White Corner Hub</h2><p>Sign in to continue</p><input name="email" type="email" placeholder="Email" required><input name="password" type="password" placeholder="Password" required><button class="primary" [disabled]="busy()">Sign in</button><div class="message">{{message()}}</div></form></div>`,styles:[`.wrap{position:fixed;inset:0;background:#eef1f5;display:grid;place-items:center}form{width:390px;background:#fff;border:1px solid #e4e7ec;border-radius:14px;padding:28px}h2{margin:0 0 6px}p{margin:0 0 14px;color:#758198}input{width:100%;padding:10px;margin:5px 0;border:1px solid #d4d9e2;border-radius:8px}button{margin-top:8px;border-radius:8px;padding:9px 14px;cursor:pointer}.primary{background:#116dff;color:#fff;border:0}.message{margin-top:10px;color:#5f6b7a;font-size:12px;line-height:1.4}`]}) export class LoginComponent{message=signal('');busy=signal(false);constructor(private auth:AuthService){}async submit(e:SubmitEvent){e.preventDefault();const f=new FormData(e.target as HTMLFormElement);this.busy.set(true);const err=await this.auth.signIn(String(f.get('email')??'').trim(),String(f.get('password')??''));this.busy.set(false);this.message.set(err??'');}}
