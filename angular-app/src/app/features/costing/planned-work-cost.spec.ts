import {describe,expect,it} from 'vitest';
import {plannedTotal,plannedWorkRows,WorkRate} from './planned-work-cost';
const rates:WorkRate[]=[['cnc',60],['assembly',30],['sanding',45],['painting',50]].map(([work_type,rate],i)=>({work_type:work_type as any,label:String(work_type),rate_gst_hour:Number(rate),sort_order:i,updated_at:''}));
describe('planned work cost',()=>{it('uses one sanding rate for product and paint sanding and excludes repaint from forecasts',()=>{const template:any={parts:[{id:'a'},{id:'b'}],estimates:{CNC:60,'Assembly:a':10,'Assembly:b':20,'Sanding:a':5,'Sanding:b':15,'Painting:First primer':10,'Painting:First sanding':10,'Painting:Second primer':10,'Painting:Second sanding':10,'Painting:Finish coat':10,'Painting:Repaint':60}};const rows=plannedWorkRows(template,rates);expect(rows.find(r=>r.key==='Sanding')?.cost).toBe(15);expect(rows.find(r=>r.key==='First sanding')?.rate).toBe(45);expect(rows.some(r=>r.key==='Repaint')).toBe(false);expect(plannedTotal(rows,false)).toBe(90);expect(plannedTotal(rows,true)).toBe(129.99);});it('keeps an incomplete total when a planned minute or rate is blank',()=>{const rows=plannedWorkRows({parts:[{id:'a'}],estimates:{}} as any,rates);expect(plannedTotal(rows,false)).toBeNull();});});
