# Shop Floor production rollout — 12 September 2026

Owner explicitly authorised the rollout with “go production”. Release branch is based on current main `beac8ce`; unrelated local edits are excluded. PR #61.

## Database

The GitHub Actions token returned HTTP 403 for the read-only Supabase Management database endpoint (run 34688413592). No SQL was applied by that run. The release instead used the owner's existing authenticated Supabase SQL Editor for project `zgvnrpspwluapaxnycrg`.

Read-only preflight: Sanding constraint present, migration not registered, zero Shop Floor tables. The exact SQL generated by `node .github/scripts/apply-shop-floor.mjs --print-sql` was pasted into the editor, copied back and compared for full equality before execution. The migration and registry insert ran in one transaction, using the same locally rehearsed wrapper.

Separate read-only postflight returned:

| Check | Result |
|---|---|
| Migration `20260912000100` registered | true |
| Shop Floor tables with RLS enabled | 6 |
| Anonymous EXECUTE on `wc_shop_command` | false |
| Authenticated EXECUTE on `wc_shop_command` | true |
| Production status guard enabled | true |

No production work sessions, orders, customer records, courier bookings or emails were created/changed as test data. Migration adds the approved tables/functions and production guard.

The temporary Management API steps added to the frontend workflow were removed after the authorised SQL Editor application and verification. Existing publication/deployment steps and change guards remain. The new isolated SQL rehearsal stays in CI. The helper script remains available for targeted application when an appropriately scoped Management token is available; never replace this with an unreviewed db push.

## Validation

On current main plus this release: 184 Angular tests, 16 server mock tests, production build with the existing Pages base path, isolated SQL/RLS/routing tests and first/repeated execution of the migration wrapper passed. CI also passed Deno checking and existing migration/integration mocks. The initial push run failed only at the unavailable Management API; final CI/deployment outcome is tracked in PR #61.

Frontend and Edge publication follow the existing main-branch workflows. The live app requires Hub login. Full PWA cold-start and automatic realtime subscriptions are outside this pilot.
